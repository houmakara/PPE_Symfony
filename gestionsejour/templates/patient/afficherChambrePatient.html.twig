{% extends 'base.html.twig' %}
{% block body %}
	<h1 class="font-weight-light text-center">CHAMBRE DES PATIENTS</h1><br>
	<ul>
		<table class="table">
			<thead>
				<tr class="text-center">
					<th scope="col">Patient</th>
					<th scope="col">Chambre</th>
                    <th scope="col">Lit</th>
				</tr>
			</thead>
			{% for chambrepatient in chambrepatients %}
				<tbody>
					<tr class="text-center">
						<td>{{chambrepatient.getPatient.getInfo()}}</td>
                        <td>{{chambrepatient.getChambre.getId()}}</td>
					    <td>{{chambrepatient.getChambre.getNochambre()}}</td>					
                    </tr>
				</tbody>
			{% else %}
				<p>Pas de patient trouvé...</p>
			{% endfor %}
		</table>
	</ul>
{% endblock %}